<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.teams.mapper.CgDiaoduMapper">
  <select id="selectcgPlanXq" resultType="cgPlanXq">
  select a.*,b.check_time from cgplanxq a,cgplan b where
  execute_tag='未执行' and a.cgPlanId in(select cgPlanId 
  from cgplan where check_tag='审核通过')
  </select>
  </mapper>